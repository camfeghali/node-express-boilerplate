"use strict";

module.exports = function (association) {
  return Object.values(association.source.rawAttributes).filter(function (attr) {
    return attr.field === association.source.primaryKeyField;
  }).length > 1;
};