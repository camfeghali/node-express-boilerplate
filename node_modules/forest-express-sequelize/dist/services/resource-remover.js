"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _resourcesRemover = _interopRequireDefault(require("./resources-remover"));

/**
 * Kept for retro-compatibility with forest-express.
 */
class ResourceRemover {
  constructor(model, params) {
    this.remover = new _resourcesRemover.default(model, [params.recordId]);
  }

  perform() {
    return this.remover.perform();
  }

}

module.exports = ResourceRemover;